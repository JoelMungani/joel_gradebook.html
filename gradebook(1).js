gradebook.js
// Stub to fetch grade data (simulated for now)
function fetchGradeData() {
  console.log("fetchGradeData called");
  // Future: Connect to PostgreSQL and fetch data
}

// Stub to populate table with grade data
function populateGradebook(data) {
  console.log("populateGradebook called");
  // Future: Populate rows in the table with student/assignment data
}

// Call both functions for testing
fetchGradeData();
populateGradebook([]);
